#!/bin/bash
git commit --amend -m "refactor(agents): replace spec-driven dev with GOAP/ADR

Deprecates spec-driven approach in favor of GOAP, ADRs, and TRIZ.
Updates AGENTS.md with new development phases.
Modifies goap-agent/SKILL.md for Phase 1 TRIZ analysis.
Replaces .gemini/commands/spec.toml with adr.toml."
