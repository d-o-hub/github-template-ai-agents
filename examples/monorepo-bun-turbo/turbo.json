{
  "$schema": "https://turbo.build/schema.json",
  "$comment": "SCAFFOLD: tasks below are placeholders matching the example packages' scripts. Add real tasks as you grow the workspace.",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
