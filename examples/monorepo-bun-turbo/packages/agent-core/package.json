{
  "_comment": "SCAFFOLD: add src/index.ts and replace placeholder scripts with real ones for this package's responsibility",
  "name": "@example/agent-core",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist",
    "test": "bun test",
    "lint": "bunx biome check ."
  }
}
