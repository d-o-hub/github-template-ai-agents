# Core dependencies
requests>=2.33.0  # CVE-2026-25645, CVE-2024-47081, CVE-2024-35195

# Optional API integrations (all optional - free tier or no key required)
exa-py>=1.0.0  # Exa API for highlights - free tier available
tavily-python>=0.3.0  # Tavily search API - free tier available
firecrawl-py>=0.0.5  # Firecrawl extraction - free tier available
# mistralai>=2.0.0  # WARNING: Project quarantined on PyPI, disabled for security
ddgs>=6.0.0  # DuckDuckGo search - FREE, no API key required
httpx>=0.27.0  # optional faster async client (used by Jina if available)
diskcache>=5.6.0  # Disk-based caching

# Development dependencies
pytest>=9.0.3  # CVE-2025-71176: insecure temp directory
pytest-cov>=4.1.0
black>=24.3.0  # CVE-2024-21503: ReDoS, CVE-2026-32274: arbitrary file writes
ruff>=0.1.0
mypy>=1.5.0

# Type hints
types-requests>=2.33.0