#!/usr/bin/env bash
# Git commit-msg hook (installed via core.hooksPath = .githooks).
# Delegates to scripts/commit-msg-hook.sh for shared logic.
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/scripts/commit-msg-hook.sh" "$@"
